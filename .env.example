# Google Sheets (optional — app works with local CSV if not set)
# Use either a single workbook with multiple sheets, or separate sheet IDs.
# You can paste the full sheet URL; the ID will be extracted automatically.
# GOOGLE_SHEET_ID=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
# GOOGLE_SHEET_ID_PILOTS=
# GOOGLE_SHEET_ID_DRONES=
# GOOGLE_SHEET_ID_MISSIONS=
# GOOGLE_SHEET_ID_ASSIGNMENTS=
# SHEET_NAME_PILOTS=Pilot Roster
# SHEET_NAME_DRONES=Drone Fleet
# SHEET_NAME_MISSIONS=Missions
# SHEET_NAME_ASSIGNMENTS=Assignments

# Service account credentials (for 2-way sync). Create in Google Cloud Console,
# enable Google Sheets API, then download JSON and save as credentials.json.
# GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# Use local CSV when true; set to false to use Google Sheets when IDs/creds are set
USE_LOCAL_CSV=true

# Test read+write: python scripts/test_sheets_integration.py

# Optional: OpenAI for richer NL (not required; rule-based works)
# OPENAI_API_KEY=sk-...
